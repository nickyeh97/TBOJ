# CLAUDE.md

本文件提供給 Claude Code（及所有 AI 協作者）在此 repo 工作時的指引。

## 語言慣例

- 與使用者的對話、文件、commit 說明主體，一律使用**繁體中文**（台灣用語）。
- 程式碼識別字（變數、函式、資料表欄位）使用英文；註解可用中文。

## 專案概觀

TBOJ（The Book of James）是一個以**兒童教育**為核心的信仰實踐計畫（雅各書 1:27、2:17）。
目前進行中的計畫案是：

> **兒童主日學整合平台（Web APP / PWA）**
> 以單一平台整合：出席預先統計、現場簽到、老師備課資源、敬拜歌單佈達、服事經歷卡、家長公告與雙向互動。
> 完整需求規格：`docs/SUNDAY_SCHOOL_PLATFORM_SPEC.md`（此為需求基準，開發前必讀）。

### 使用者規模（小規模、真實名單制）

- 孩子 47 位（兒童/幼童/幼幼三個班別）、家長約 26 位、老師約 20 位，總計約 100 人以內。
- 95% 以上使用者以**手機瀏覽器**（iOS / Android）操作 → **一律 mobile-first 設計**。
- 規模小、名單固定：不做公開註冊，不追求擴張性指標；優先考量簡單、穩定、零成本或低成本方案。

## 最高守則（開發前必讀）

`docs/DESIGN_PRINCIPLES.md` 是本 repo 的**最高守則**，任何功能設計必須先通過其檢核。與本計畫特別相關的重點：

1. **兒童個資最小化**（紅燈規則 #6）：只蒐集必要欄位；兒童資料絕不用於商業用途、不外流。
2. **不做比較式排行**（紅燈規則 #1）：「服事經歷卡」是孩子**自己的成長軌跡**（加 6:4），不得演變成孩子之間的排名或比較。
3. **不做成癮式留存機制**（紅燈規則 #2）：出席統計是行政工具，不得用連續出席壓力、罪惡感通知逼迫使用。
4. **不羞辱、不標籤化**（紅燈規則 #7）：缺席紀錄僅供關懷 follow-up（未來的關懷警訊機制），不得公開呈現誰缺席。
5. 若設計與守則衝突：**立即停下、提出討論**，不得便宜行事。

## 角色與權限（三種角色）

| 角色 | 權限重點 |
| --- | --- |
| 管理者（核心同工） | 全權存取、發布教材/歌單、維護名單、審核服事資格 |
| 老師 | 現場簽到點名、檢視備課教材（影音+討論問題）、觀看舞蹈教學影片、登錄服事履歷 |
| 家長 | 填寫預先出席、預習下週歌單、查看自己孩子的服事卡、接收/回覆公告 |

權限原則：家長**只能看到自己綁定的孩子**；一位家長可綁定多位孩子（跨班別），介面需支援切換或一次勾選多孩。

## 開發優先順序（Roadmap）

- **Phase 1（急用，取代現有 Google 表單流程）**：
  1. 家長預先出席勾選（每週固定開放時段，如週三前）＋自動統計預計出席人數
  2. 老師現場簽到點名
  3. 敬拜歌單/影片發布（後台上傳歌詞、音檔或 YouTube 連結；家長端主日後可預習下週內容）
  4. 家長佈告欄（公告發布）
- **Phase 2（文化深化）**：
  5. 電子服事經歷卡（服事紀錄＋狀態：受訓中/已核定/服事完成；家長端可檢視）
  6. 公告雙向互動（家長回覆/簽到）
- **未來預留（不在現階段實作，但資料模型要預留）**：
  - LINE 機器人/通知串接（出席提醒、歌單推播）
  - 家庭延伸祭壇（主日討論題回饋與打卡）
  - 關懷警訊（連續缺席自動標示，僅供窗口關懷用）
  - User 資料表預留欄位：生日、大孩子階級（培訓中/正式領袖）、家庭關聯 ID

## 技術方向

- **型態**：Web APP（RWD）或 PWA，**不上架 App Store / Google Play**；手機瀏覽器「加入主畫面」即可使用。
- **選型原則**（因總使用者 < 100 人）：
  - 優先選擇免費額度可長期涵蓋的託管方案（如 Vercel / Cloudflare Pages + Supabase / Firebase 等），避免固定月費。
  - 影音一律**外連或嵌入 YouTube**，不自建影音儲存/串流。
  - 登入方式力求對家長最低門檻（規模小，可考慮邀請連結/簡易帳號，未來再接 LINE Login）。
- **核心資料實體（建模時參考）**：`User`（含角色）、`Child`（含班別、階級）、`Family`（家長-孩子關聯）、`AttendancePlan`（預先出席）、`CheckIn`（現場簽到）、`Lesson`（每週教材/討論問題/帶領提示）、`Song`／`Playlist`（敬拜歌單）、`ServiceRecord`（服事經歷＋狀態）、`Announcement`／`Reply`（公告與回覆）。

## Repo 結構

```
README.md                              # TBOJ 總計畫願景與七階段 Roadmap
CLAUDE.md                              # 本文件
docs/DESIGN_PRINCIPLES.md              # 最高守則（設計檢核，必讀）
docs/SUNDAY_SCHOOL_PLATFORM_SPEC.md    # 兒主平台需求規格書（需求基準）
docs/DISCORD_ROUTINE.md                # Discord 進度通知流程
```

## 工作流程慣例

- **分支**：功能開發在 `claude/*` 或 feature 分支進行，不直接 push `main`。
- **Commit**：訊息以繁體中文為主，清楚描述「做了什麼、為什麼」。
- **Discord 通知**：push 到 `dev` 分支會觸發 GitHub Actions 自動發送進度通知（見 `docs/DISCORD_ROUTINE.md`）；Webhook URL 存於 GitHub Secret `DISCORD_WEBHOOK_URL`，**絕不寫入 repo**。
- **機密**：`.env`、`*.secret` 已被 gitignore；任何金鑰、Webhook、兒童個資都不得進版控。
